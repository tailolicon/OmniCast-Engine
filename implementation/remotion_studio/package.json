{
  "name": "omnicast-remotion-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Remotion compositions for OmniCast animated scenes (intro, stat, quote, outro). Rendered per-scene by render_real_video via remotion_render.py — additive, does not replace the ffmpeg pipeline.",
  "scripts": {
    "studio": "remotion studio",
    "render": "remotion render"
  },
  "dependencies": {
    "@remotion/cli": "4.0.347",
    "react": "19.0.0",
    "react-dom": "19.0.0",
    "remotion": "4.0.347"
  },
  "devDependencies": {
    "@types/react": "19.0.0",
    "typescript": "5.8.2"
  }
}
