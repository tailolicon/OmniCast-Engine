"""Ingest layer: pulling source media into OmniCast from external platforms.

Distinct from `omnicast.discovery`, which finds *topics* to write about. Ingest
brings in an actual finished video that OmniCast will re-voice and republish.
"""
