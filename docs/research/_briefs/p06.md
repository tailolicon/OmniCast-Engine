Bạn đã nghiên cứu Pixelle-Video + MoneyPrinterTurbo + short-video-factory phiên trước (report `docs/research/REFS_SB_06_ShortVideoEngines.md`).
Verifier đã soát: đọc `docs/research/REFS_SB_00_VERIFY.md` mục §2.6, §3.2 (V5) và §4.6.

Nhiệm vụ — CHỈ sửa file `docs/research/REFS_SB_06_ShortVideoEngines.md`:
1. Vá V5: mọi khối Pixelle gắn nhãn VERBATIM đang là outline/`…` → chép FULL chuỗi thật.
2. Thêm section cuối `## ROUND 2 — VERBATIM FULL (theo VERIFY §4.6)`: chép FULL string
   trong `topic_narration.py`, `content_narration.py`, `image_generation.py`,
   `video_generation.py` (Pixelle); phần search-terms + social metadata của MPT `llm.py`;
   grep toàn bộ template string trong `src/` của short-video-factory để xác nhận có/không
   prompt domain (liệt kê kết quả grep). CẤM `…` trong khối verbatim.
3. Cập nhật "Đã đọc". Không sửa file khác. Xong in ra: số file đã chép full.
