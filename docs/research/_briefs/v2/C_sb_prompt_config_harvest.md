Đọc trước: `docs/research/_briefs/v2/COMMON_V2.md`. Nhóm này LỆCH TRỌNG TÂM: 90% Tầng 1.

# Nhóm C — HARVEST PROMPT/CONFIG 10 repo storyboard đã mổ đợt 1:
`_refs/ArcReel`, `_refs/AIComicBuilder`, `_refs/StoryGen-Atelier`, `_refs/Jellyfish`,
`_refs/LocalMiniDrama`, `_refs/waoowaoo`, `_refs/seedance-2.0`, `_refs/Orkas-VideoStudio`,
`_refs/hyperframes`, `_refs/short-video-factory`

Đợt 1 (docs/research/REFS_SB_01..07) đã phân tích CƠ CHẾ tốt nhưng phần trích VERBATIM
chưa đủ làm chuẩn tinh chỉnh. Nhiệm vụ: quét lại 10 repo CHỈ để lập KHO PROMPT/CONFIG
đầy đủ — KHÔNG phân tích cơ chế lại (trừ khi thấy cơ chế mà REFS_SB đợt 1 BỎ SÓT hẳn —
khi đó ghi vào mục riêng "SÓT ĐỢT 1").

Quét có hệ thống MỖI repo: mọi file/thư mục tên chứa prompt|template|instruction|system|
golden|preset|config|constant|default|rubric|guide (.py .ts .js .md .yaml .json .toml) +
grep chuỗi dài gửi LLM trong code. Với mỗi mục: trích nguyên văn ĐẦY ĐỦ (prompt dài vẫn
chép đủ — đây là kho tra cứu), file:dòng, 1 câu nó dùng ở bước nào.

Đối chiếu nhanh (1 dòng/mục) với chỗ tương ứng OmniCast nếu thấy rõ (writer/critic/
prompt_builder/clips/frames/style_lock/edl); không thấy rõ thì ghi "chưa map".

Output: `docs/research/V2_C_SBHarvest.md` (chấp nhận file RẤT DÀI — đây là kho tra cứu).
